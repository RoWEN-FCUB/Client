import { GetObservTextPipe } from './get-observ-text.pipe';

describe('GetObservTextPipe', () => {
  it('create an instance', () => {
    const pipe = new GetObservTextPipe();
    expect(pipe).toBeTruthy();
  });
});
