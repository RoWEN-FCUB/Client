import { ShortSerialPipe } from './short-serial.pipe';

describe('ShortSerialPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortSerialPipe();
    expect(pipe).toBeTruthy();
  });
});
