import { GetObservationsPipe } from './get-observations.pipe';

describe('GetObservationsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetObservationsPipe();
    expect(pipe).toBeTruthy();
  });
});
